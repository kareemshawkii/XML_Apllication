#include "json_converter.h"
using namespace std;
string convertXMLtoJSON(const string& xmlContent);
// Converts XML content to JSON format and returns it.

string convertJSONtoXML(const string& jsonContent);
// Converts JSON content back to XML format (optional).
